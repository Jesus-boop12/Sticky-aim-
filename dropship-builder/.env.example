# Optional. Without a key the builder still makes a complete store using its
# built-in copy templates. With a key, Claude writes the store copy and can read
# product pages that block ordinary downloads.
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides
PORT=5174
DROPSHIP_MODEL=claude-opus-5
