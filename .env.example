# Optional. Without it the app still works - AI features fall back to a local
# heuristic parser and the script generator is fully offline/deterministic.
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides
PORT=5173
STICKY_AIM_MODEL=claude-opus-5
